seed: 0
prefix: runs
device: cuda
disjoint_train_ratio: 0.2
val_ratio: 0.15
test_ratio: 0.15
num_epochs: 300
eval_every_n_epochs: 25
save_every_n_epochs: 50
target_edge: ["Compound", "CrC", "Compound"]
fetch_smiles: true
with_embeddings: false
batch_size: 32
use_peft: false
peft_in_dim: 768
run_name: hetionet_without_peft
